global:
  scrape_interval: 2s
  evaluation_interval: 2s

scrape_configs:
  - job_name: dispec
    # The DiSpec server runs on the host (it needs the GPU); scrape it from the container.
    static_configs:
      - targets: ["host.docker.internal:8000"]
